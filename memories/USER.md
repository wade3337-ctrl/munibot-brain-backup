Brent Beller, Contract Admin GSTS. Owns muni budgets in TRIM IT (UserID 40). Style: concise, data-forward, tables over prose. Prefers to SIFT FIRST when receiving bulk files. Wants proactive flagging of data-quality issues.
§
TRIM IT query: `/opt/data/home/trimit-query.sh` (NOT `~/home/...`). Read-only, ~24h behind live. UI proposal numbers = LegacyRef, NOT ProposalID.
§
Jason Wade (Skipper/COO) jwade@gstsinc.com = bid reviewer. "Send it to me" = execute immediately, no draft (self-delivery). Also approved: gilligan.gsts@gmail.com. Draft-approve only for cities/Brent/external.
§
Emailing TRIM IT files: ALWAYS ask Brent about naming/format BEFORE sending. His default = name files by project name. TRIM IT ImagePath filenames are useless storage URLs — use Desc1 or project name to rename.
§
NEVER use another city's invoicing data as a proxy when a city's own data is missing. Hard rule. Flag the gap — never fill it with another entity's data.
§
Jason (Skipper/COO) tests bot directly. Asks about sales reps (Scott Griffiths). Brent sends tasks via email and expects review + questions before execution. Sends multi-part tasks (e.g. update pricing tool + record SOP separately) with clear separation rules.
§
Brent audits data provenance on deliverables — every column must be traceable. Inventory totals must match City's RFP, not TRIM IT.